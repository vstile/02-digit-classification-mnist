# 02-digit-classification-mnist
TensorFlow/Keras project for classifying MNIST handwritten digits (0–9). Includes a compact CNN in a Jupyter notebook, a clean data pipeline, training/evaluation on the standard split, and utilities to test custom images (28×28 grayscale). Reproducible seeds, saved weights, and simple prediction/visualization cells included.
